export type ProductRequire={
     // 商品要求id
     id:number,
     // 商品要求
     name:string,
     // 是否可用
     status?:number,
     // 创建时间
     createTime?:string,
     // 更新时间
     updateTime?:string
}